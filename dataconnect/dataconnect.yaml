specVersion: "v1beta"
serviceId: "knowledge-base"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "knowledge-base-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
